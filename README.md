# Svelte App

Minimal Vite + Svelte app scaffolded in this folder.

Quick start:

```bash
cd /Users/daniel.maldonado1/Desktop/svelteApp
npm install
npm run dev
```

Open the shown local URL in your browser.
